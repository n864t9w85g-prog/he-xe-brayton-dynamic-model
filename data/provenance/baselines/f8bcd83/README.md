# f8bcd83 immutable model baselines

Source commit: `f8bcd833e816eb681982b7dd04364e4b856948e3`.

`final_steady_24a.slx` is the byte-identical authority for the radiator A1 exploration contract. `final_dynamic_24a.slx` is retained only as historical recovery evidence; its presence does not reinstate any conclusion obtained from the superseded dynamic-model state.

These SLX files are recovered as opaque Git blobs. They were not loaded, unpacked, simulated, or edited during recovery.

`paper_reproduced = false`

`formal_promotion = false`
