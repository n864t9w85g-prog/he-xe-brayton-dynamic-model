# 交付边界约束 v4 — 论文5.4动态复现（精简可执行版）

> 配套文档：`决策自律准则.md`（证据分级标记 ✅/⚠️/❓/❌，本文件引用论文出处时必须
> 使用同一套标记）、`验收标准_论文5.4.md`（数值/曲线验收依据）。
> 本版本替代此前的两个版本：初版《交付边界约束》遗漏了具体黑名单和探索区/交付区
> 的操作细则；v3.1 补上了细则但引入了一处会直接判物理机制违规的硬伤，且规则之间
> 互相打架。本版本修复致命伤、拆分白名单层级、统一信号记录规则，并有意舍弃了与
> 当前任务规模不匹配的重型配置管理流程（哈希清单、五层目录、机器可执行场景表等）
> ——这些留到模型真正跑通、需要交给别人复现时再补。

---

## 0. 一句话目标

复现论文《空间锂冷堆He-Xe布雷顿循环发电系统优化设计与运行特性分析》第5.4节的
动态仿真结果（5.4.1启动瞬态、5.4.2变工况响应：±5%转速扰动、TAC负载扰动、反应性
扰动），并保证后续能在同一模型基础上复现更多变工况测试。数值/曲线的允许误差按
`验收标准_论文5.4.md` 执行。

**最终交付的 Simulink 模型和支撑代码，只能包含论文模型本身需要的物理部件和该部件
自身的物理耦合，不允许包含任何为了让数值对上而额外加装的修正块、外挂补丁脚本、
控制器/校正反馈、或调试专用的辅助子系统。**测试阶段可以自由添加这些东西定位问题，
但它们不能进入最终交付路径，也不能被最终交付的模型引用。

---

## 1. 开工前的两件事（Step 0）

1. **带时间戳备份**：`final_dynamic_24a_backup_YYYYMMDD_HHMMSS.slx`。本次会话结束
   前不得覆盖或删除，它是后续回滚的基准。无法备份时先停下说明原因，不要跳过。
2. **跑一遍现状，记录卡在哪**：正式改动前，用当前模型的原始配置运行一次，记录它
   现在在哪一步报错/发散/不收敛。这是为了后面能分清"这个问题模型本来就有"还是
   "这次改动引入的"——不需要新开进程、算哈希、审计回调这类仪式，跑一次、记一下
   现象就够。

---

## 2. 模型结构盘点（Step 1）

打开备份文件，列出 `final_dynamic_24a.slx` 顶层所有子系统的精确名称（大小写/空格/
下划线需与 Simulink 一致），对每一个标注：

- 能归入第3.1节"物理架构层"清单 → 归类为该部件
- 不能归入 → 标记为"未归类"，**先看一眼它实际连接了什么信号、做了什么运算，
  再判断是不是问题**——名字可疑（比如带 Controller/Patch/Fix 字样）只是需要
  多看一眼的信号，不是自动判违规的依据；反过来，名字看起来正常（比如改名成
  `PowerBalance` 的外挂控制器）也不能只凭名字放行。

盘点清单作为报告的一部分输出，确认后再进入修改步骤，不要边看边改。

---

## 3. 交付边界：拆成两层，不是一份名单

v3.1 把"顶层该有哪些部件"和"部件内部该用哪些基础块实现"混在一份名单里，导致
Gain、Lookup Table 这类正常实现方程要用的基础块被误伤。这里明确拆开：

### 3.1 物理架构层（顶层子系统只能是这些）

参照论文图2.1系统组成和当前模型 `system_root` 的实际结构：

- `reactor`（反应堆，含反应性输入信号 `rho_fw`——用于按论文图5.24/5.33的时序
  注入开环、预先给定的反应性扰动曲线，这是复现变反应性工况必需的输入端口，
  不是控制器）
- `TAC`（透平-发电-压缩一体机，含负荷输入信号 `Pload_fw`——用于按论文图5.31/
  5.28的时序设定开环负荷曲线，同理不是控制器）
- `recuperator`（回热器）
- `IHX`（中间换热器）
- `precooler`（预冷器/冷却器）
- `radiator`（辐射散热器；当前模型内实际命名为 `rediator`，是否统一拼写由你
  决定，agent 不擅自改名）
- 满足第3.4节条件的只读信号记录

不在此列表、且找不到论文出处的新增顶层子系统，一律不允许。

### 3.2 方程实现层（部件内部的基础块，不受3.1节名单约束）

Gain、Sum、Integrator、Lookup Table、MATLAB Function 等基础块是实现物理方程
的正常手段，只要能说明"这是论文哪个方程/哪张表"（按第5节出处要求），就允许
存在于对应部件内部，不因"不在顶层白名单里"被判违规。**风险不在于用了什么
基础块，而在于这个基础块实现的是论文的方程，还是为了拟合曲线临时加的修正。**

### 3.3 明确排除——这些不是论文物理机制的一部分

以下内容，无论顶层还是任何部件内部，一律不允许，即使 agent 认为它"有助于
仿真稳定或收敛"：

- **外部电力系统**：PMAD/电网/电池/逆变器
- **测量层伪影**：传感器模型、噪声注入、测量滤波器
- **未经论文支持的高维离散化**：三维CFD、FEA；论文本身采用的一维分区/集总
  参数节点（如换热器若按论文2.4.3节做了分区设计）**不在此列，允许保留**
- **安全联锁**：scram逻辑、卸压阀、应急冷却触发
- **数据驱动型修正**：人工神经网络、基于外部数据训练/拟合的查表或估计器
- **外挂式控制/校正反馈**：为了让曲线更快收敛或误差更小而额外加装的
  PID、增益反馈、状态回注——见3.4节的判断标准

补丁式命名（`apply_*`/`patch_*`/`fix_*`/`activate_*`/`build_*_lookup`）是需要
多看一眼的信号，不是自动删除的依据：如果这个功能确实是论文要求的（例如压气机
修正坐标法，对应论文2.4.2节的部件建模方法），应重构为一个有正式名字、落在
3.1或3.2节里的部件模块，而不是留作一个外挂在别的模块之外的"apply"脚本。

### 3.4 反馈闭环 vs 物理耦合——这条是本文件相对v3.1的核心修复

**v3.1原表述"任何形式的反馈闭环都不允许"是错的，会把论文要复现的物理机制本身
判违规**，必须废止，改为以下判断标准：

论文模型本身就存在物理耦合，这些是被建模对象的固有规律，不受黑名单约束：

- **转子动力学**：转速的变化率由(透平功率-压缩机功率-负载功率)与转动惯量、
  转速共同决定（对应论文 Eq.5.17-5.18；agent 执行时应自行核对原文该处公式
  的完整形式与符号定义，不要凭记忆默认某个具体表达式）。转速不是被"控制"出来
  的，是功率平衡的自然结果——这正是图5.22、图5.27-5.30"动态稳定点"分析的
  基础，删掉它论文这部分结果就无法复现。
- **反应堆温度负反馈**：燃料/冷却剂温度变化引起反应性变化，进而影响堆芯功率
  （论文原文第103页明确写"在堆芯温度负反馈机制的自我调节下，反应性迅速
  下降……"），这也是论文物理模型的一部分。

**真正应该禁止的**，是为了缩小论文曲线误差、或强行让数值收敛/稳定，而**额外
加装**的控制器、校正反馈、状态回注——判断标准不是"有没有反馈"，而是
**"这个反馈是论文方程本身给出的，还是这次改动为了凑数值新加的"**。拿不准的，
按第5节出处要求处理：说不出论文章节/公式号，就先别加，向我确认。

### 3.5 信号记录——统一成一条规则，不再自相矛盾

v3.1里"必要的信号记录"和"Scope一律不允许"互相打架。改成唯一规则：

最终交付模型中，允许保留的信号记录**必须同时满足**：只读（不接入任何反馈
回路）、不影响仿真数值结果（不修改被记录信号的上下游）、能对应论文中"结果
输出"或用于跟验收标准比对的表述。

除此之外的 Scope、To Workspace/From Workspace、To File/From File、Signal
Builder、Manual Switch、Slider Gain、Test Point/Assertion/Verify 等交互式或
调试专用块，一律留在探索区，不进入交付模型。

**禁止以调试之名行建模之实**：不能用 Signal Builder 替代应有的开环负荷/反应性
曲线实现，不能用 Manual Switch 实现"运行模式切换"这类本应由方程或参数调度
完成的逻辑。

---

## 4. 探索区 vs 交付区

- `tests/`、`tmp/` 下允许自由添加断点、诊断输出、参数扫描脚本、临时对照子系统，
  不受3.3/3.5节约束。
- 探索区验证有效的修正，要么能证明对应论文建模内容（引用章节/公式/图表后正式
  并入3.1或3.2节范围），要么就不能进入交付物——不存在"先放进正式模型、回头
  再清理"的中间状态。
- 交付前需确认：探索区产生的脚本/子模型，都没有被 `final_dynamic_24a.slx` 及
  其依赖链引用。

---

## 5. 出处要求——分三类，不绝对化

v3.1"每个非默认参数都要有论文出处"太绝对，论文不会写全部数值实现细节（求解器
容差、插值方法这些）。分三类处理：

| 类型 | 处理方式 |
|---|---|
| **物理建模选择**（部件结构、方程形式、修正模型） | 必须能指出论文章节号/公式号/图表号，找不到出处默认不保留 |
| **纯数值实现细节**（求解器容差、插值方法、数值防护、初始猜测值） | 不强求论文出处，但要说明"这是数值实现选择，不改变物理方程"，供你判断是否合理 |
| **标定/拟合参数**（为了让曲线更接近论文而调的系数） | 默认不允许，除非单独说明理由并获得你的批准 |

对物理建模选择类的出处引用，按 `决策自律准则.md` 的证据分级标记（✅本轮核实/
⚠️沿用未复验/❓推断/❌无依据）标注，不要笼统写"文"或"图"了事——参照上一轮
对验收标准文档的核对经验，"从图上读出来的数"和"论文文字明确给出的数"证据
强度不一样，必须区分。

---

## 6. 交付物

交付 = `final_dynamic_24a.slx`（或明确指定的新文件名） + 它直接依赖、且按第5节
能说明来源的物性/常量脚本（命名体现"这是论文里的什么"，而不是"对什么做了
什么操作"） + 第7节的模型清单。

不属于交付物、留在 `tests/`/`tmp/`、不能被交付模型引用：任何 `.bak`/
`.original`/`.slxc`/`.r2024a` 等版本残留（Step 0的备份本身也不算交付物）、
调试/试探/单元测试脚本、`apply_*`/`patch_*`/`fix_*` 类未被正式重构的补丁脚本。

**关于以后扩展更多变工况测试**：新场景应该通过 `tests/` 下的脚本改变输入信号
（`rho_fw`、`Pload_fw` 这类已有的开环输入端口）去驱动同一个交付模型，不需要
改动模型拓扑本身。已知的4类场景参数（已在`验收标准_论文5.4.md`中核实）：

| 场景 | 输入端口 | 时序 | 数值 |
|---|---|---|---|
| 变负载 | `Pload_fw` | 0/500/1500/2500s | 1000→950→1000→1050 kW |
| 变反应性 | `rho_fw` | 0/500/2000/3500/5000s | 0→+0.0001→-0.0001→0→+0.0003 |
| 变转速(稳定点) | 转子初值扰动 | 稳定运行500s后 | 55090rpm ±5% |
| 变转速(不稳定点) | 转子初值扰动 | 稳定运行500s后 | ~34780rpm ±5%（该数值来自5.3节，未在本轮核实范围内，使用前建议核对） |

---

## 7. 交付前自查清单

任务完成前必须输出模型清单：

| 模块/脚本名 | 归类（3.1物理架构层 / 3.2实现层 / 未归类） | 论文出处 | 证据标记 |
|---|---|---|---|

"未归类"或找不到出处的条目，单独列出，不能默认合并进交付、也不能默认删除，
由你做最终判断。同时确认 Step 0 的备份仍完整存在、探索区文件未被交付模型引用。

---

## 8. 发现违规怎么处理

发现3.3/3.5节列出的黑名单内容，或未经批准的新增顶层子系统：

1. 停止继续做其他改动
2. **不要立即删除**——移到 `tmp/quarantine/` 之类的隔离位置，保留证据
3. 在报告中说明：加了什么、为什么判定违规、建议怎么处理
4. 等你确认后再执行删除或保留；确实需要回滚模型状态时，回到 Step 0 的备份
5. 确认后，仅用第3节范围内的内容继续完成任务

---

## 9. 给 Codex 的收尾指令

> 在标记任务完成之前，请先输出第7节要求的模型清单，并明确说明本次改动中是否
> 新增了任何不在第3.1/3.2节范围内的模块或脚本，是否出现了第3.3节黑名单内容，
> 或第3.5节之外的信号记录/调试块。如果有，按第8节流程处理，不要自动保留也
> 不要自动删除，先向我确认。清单中每条论文出处按`决策自律准则.md`的证据分级
> 标记标注。
