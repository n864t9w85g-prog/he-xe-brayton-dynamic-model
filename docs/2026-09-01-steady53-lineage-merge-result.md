# 2026-09-01 稳态基线谱系组合 500 s 诊断结果

**状态：** 探索区单次实验已完成；不晋升正式模型  
**批准方案：** A（冻结 f8bcd83 方程/工况参数 + 根模型完整 40 状态初值向量）  
**原始证据目录：** `tmp/steady53_lineage_merge_20260901_A/`

## 1. 合同与执行事实

- ✅ 根模型 SHA-256：`a93cd94b6c5a0c941fe9eed40d8aea779019cbdb26fe2558df65f7c81c9dc159`。
- ✅ 冻结模型 SHA-256：`0532e9ddf2deb7ef5e40cc1b8e619c44ea7afd36b00d807d118f4cd812a5a391`。
- ✅ 候选模型 SHA-256：`5e540941d24d7488e0ef8420b3c6d1435b898dae681ddbfeacdd6abf13cf2ab2`。
- ✅ 40 个共同积分器均逐项采用根模型初值；其中 39 项发生数值变化，`TAC/rotor/N_rpm_Integrator` 在两版中原本均为 `55090`，因此 1 项不变。
- ✅ 根模型 `Unit Delay1` 不属于上述 40 状态向量，候选保持冻结模型值。
- ✅ 块清单、信号连线、非初值对话框参数及求解器参数审计均未变化。
- ✅ `run_steady53_case` 调用计数为 1，重试计数为 0；运行成功到达 500 s。
- ✅ `paper_reproduced=false`、`author_initial_state_identified=false`、`formal_promotion=false`。

以上事实来自同目录的 `candidate_audit.json`、`run/run_status.json` 和 `analysis.json`。

## 2. 图 5.19 四面板结果

| 面板 | 候选方向序列 | 论文固定方向 | 非平坦 | 候选 RMSE (kW) | 冻结基线 RMSE (kW) | RMSE 变化 (kW) |
|---|---|---|---:|---:|---:|---:|
| 反应堆功率 | `rise, fall` | `fall` | 是 | 189.448 | 237.551 | -48.103 |
| 涡轮功率 | 空序列（固定容差下无有效方向段） | `rise` | 是 | 64.661 | 63.964 | +0.697 |
| 压气机功率 | `rise, fall` | `fall, rise` | 是 | 345.401 | 29.450 | +315.952 |
| TAC 电功率（论文效率口径） | `fall, rise` | `rise, fall` | 是 | 298.542 | 34.544 | +263.998 |

✅ 四个面板的峰峰值均超过预先冻结的非平坦阈值，但方向匹配数为 0/4。机械分类为：

`lineage_initial_state_split_not_supported`

## 3. 可讲述结论与边界

- ✅ 该候选不是“仍然平坦”；完整根模型初值向量确实激发了明显瞬态。
- ✅ 这些瞬态没有恢复论文图 5.19 的方向结构，尤其压气机与 TAC 电功率方向恰好相反，并且 RMSE 明显恶化。
- ❓ 因此可以否定“只需把根模型 40 状态初值搬到冻结工况模型，就能解释论文曲线”的单因假设；不能据此判定剩余根因究竟是作者未公开初值、边界/参数谱系、方程实现还是多项共同作用。
- ✅ 本实验没有修改正式 `final_steady_24a.slx`、正式 MAT、物性函数或冻结 provenance；运行后 SHA-256 与运行前记录一致。
- ✅ 本结果不是论文复现，也不是正式模型修复，不触发第二候选、参数扫描或 14000 s 延伸。

## 4. 原始证据 SHA-256

| 文件 | SHA-256 |
|---|---|
| `candidate_audit.json` | `771a5476d5fa85e118df51bf99d5f81a94119286c3c9799e6f61951b62f79034` |
| `run/raw_result.mat` | `fb47a9f32ce54da0dbb3b76a629238dd52378d98892403a7e4dd3847b1b9209a` |
| `run/curves.csv` | `6c90777458b58f5acd196d8095d7131068b0f33f95b6f39dd58b296352692394` |
| `run/run_status.json` | `989f065abb71830e2d0c7e093eb2ec0698f83eccb35894971bfe82eb3cc00664` |
| `analysis.json` | `c706d8dd113eb06c0d4f1f80c59384c3c977fd139123a6e1d83e0aed8fb26ca3` |
| `comparison.csv` | `926e2c02d618e976f28fb75f7449a4017efcc0deb9ffc1e2901730f80bac6fcb` |
| `figure5_19_lineage_merge.png` | `07ba5a48afafee60f4625831cfcebc8484d59d5a93a93cd7d55e2f291a8a4918` |

## 5. 失败路径的功率分解与方程定位

本节是在不重新仿真、不修改模型的条件下，对同一份
`run/raw_result.mat` 做的只读复算。物性计算固定调用冻结 runtime 的
`HeXe_property_simulink.m`，没有调用根目录下可能被路径遮蔽的同名函数。

### 5.1 压气机功率反向峰值由入口温度路径主导

✅ 活动压气机功率可由原始信号重建为

```text
Wc = mdot * cp(Tin, Pin) * (Tout - Tin)
```

225 个保存样本上的最大相对重建误差为 `2.533e-8`。关键点为：

| 量 | 0 s | 18.048012 s（压气机峰值） | 500 s |
|---|---:|---:|---:|
| 压气机功率 / kW | 1205.853302 | 1673.096088 | 1304.885671 |
| 压气机入口温度 / K | 397.674835 | 552.342113 | 430.478843 |
| 压缩温升 / K | 193.498844 | 268.618711 | 209.427390 |
| He-Xe 流量 / kg/s | 11.982029 | 11.982029 | 11.982029 |
| 入口 `cp` / J/(kg K) | 520.098661 | 519.821508 | 520.006298 |
| 压比 | 2.362024 | 2.362024 | 2.362024 |
| 压气机查表流量比 | 0.995185 | 0.995185 | 0.995185 |
| 压气机查表转速比 | 1.000000 | 1.000000 | 1.000000 |

✅ 从 0 s 到 18.048012 s，压气机功率倍率的精确乘积分解为：

```text
1.38747896
= 1.00000002  (流量倍率)
* 0.999467115 (cp 倍率)
* 1.38821868  (温升倍率)
```

因此该候选早期压气机功率上冲不是流量、压比、查表转速或 `cp` 的变化造成；
直接作用量是入口温度抬升后产生的压缩温升扩大。

✅ 论文效率口径电功率采用 `0.98*(WT-Wc)`。0 s 至 18.048012 s：透平功率
增加 `11.548381 kW`，压气机耗功增加 `467.242786 kW`，所以电功率变化为
`0.98*(11.548381-467.242786)=-446.580516 kW`。这直接解释了候选电功率为何
与论文方向相反。

### 5.2 入口温度上冲来自不相容的热状态拼接

✅ Simulink 官方 API 追线确认：压气机入口温度等于
`precooler/precooler_2/T_h2_out_Integrator`。该状态从
`397.674835 K` 上升到 18.048012 s 的 `552.342113 K`，随后回落到 500 s 的
`430.478843 K`。

✅ 官方 API 读取的该区方程块对应：

```text
dTho/dt = [mdot*cp*(Tbar_h-Tho)
            - 0.5*h_h*A*(Tbar_h-Twall)]
           / (0.5*m_h*cp)
```

其中候选保持冻结参数 `h_h=1384.3 W/(m^2 K)`、`A=12.256 m^2`、
`m_h=0.0148 kg`。把根模型状态向量的同区三个初值
`Tbar_h=471.153329 K`、`Tho=397.674835 K`、`Twall=425.212725 K`
代入冻结方程，得到：

| 项 | 根状态向量代入冻结方程 | 冻结模型自身初值代入冻结方程 |
|---|---:|---:|
| 流动焓项 / W | 457756.493 | 393252.522 |
| 半区热侧换热项 / W | 389713.700 | 393252.007 |
| 状态方程残差 / W | **+68042.792** | **+0.516** |
| `dTho/dt` / K/s | **+17685.059** | **+0.134** |

保存轨迹第一个 `80.19 us` 区间的有限差分斜率为约 `+15386 K/s`，与上述
瞬时方程方向和量级相符。

❓ 因而本实验能否定的是“根模型 40 状态向量可直接作为冻结模型初态”这一谱系拼接
假设。压气机和电功率的反向大峰值是该不相容拼接的可追踪后果；这不是证明预冷器
参数、冻结初值或作者初值本身已经正确，也不是证明其余曲线差异只有这一个原因。

## 6. 当前正式根模型与冻结证据基线不是同一工况

✅ 本地 Git 谱系和 Simulink 官方 API 给出以下直接事实：

- 当前 `main`/本分支包含提交 `4e48ca6`，但不包含提交 `1960aa1`；后者的提交说明
  和实际模型改动均是把稳态 TAC 部件转速从 `66100` 修正为 `55090 rpm`，目前仍可
  从本地分支 `codex/steady53-14000s` 追溯。
- 当前根 `final_steady_24a.slx` 的 `TAC/Constant=66100`，并直接驱动压气机和
  透平；冻结模型对应值为 `55090`。
- 当前活动 `hexe_compressor_lookup.mat` 的 `N_design=55090`，实际速度断点为
  `[0.9, 1.0, 1.01, ..., 1.10]`。因此根模型查表坐标为
  `66100/55090=1.199854783`，超出上界 `1.1`；两个压气机二维表的
  `ExtrapMethod=Clip`，实际只能使用最高速度行。
- 根模型负荷常数为 `1087e3 W`；冻结证据基线为 `1000.21e3 W`。
- 除 40 项 `InitialCondition` 外，两版还有预冷器、回热器和散热器参数差异；不能把
  根模型状态和冻结参数自由组合后称为同一物理工况。

✅ 这说明当前仓库根 `final_steady_24a.slx` 不能直接作为论文 §5.3 名义工况的
已验证模型：至少实际转速和负荷就不等于论文对比口径。冻结 f8bcd83 快照可作为
已固化的探索证据基线，但其换热/散热器参数来源、能量一致性和图线复现仍未全部通过，
不能因此自动晋升为正式正确模型。

## 7. 对主线的修正：先恢复论文试验协议，再比较图 5.18/5.19

✅ 论文 PDF 第 105 页（印刷第 90 页）§5.3.1 明确说明：为分析部件内部参数变化，
使用稳态时各部件输入参数作为其对应的初始参数，以单独部件为研究对象，观察模块达到
稳态前的输出变化。PDF 第 106 页（印刷第 91 页）又把图 5.19 称为“系统主要部件在
稳态前的功率变化过程”。

✅ 同一论文 PDF 第 97 页（印刷第 82 页）对透平模型的说明是：入口温度、压力、
He-Xe 质量流量和 TAC 转速为输入，出口温压、膨胀功率和流量为输出；当前冻结模型的
压气机和透平功率路径也都是上述输入的代数计算。在转速固定时，若其余输入在整个运行
期间也保持恒定，透平和压气机功率不会形成图 5.19 的明显数百秒瞬态。

❓ 因此原文“以单独部件为研究对象”不能被进一步解释成“切断全部系统耦合并在全过程
保持恒边界”。论文没有交代图 5.19 中透平/压气机入口历史究竟来自全系统联算、部件间
顺序回放，还是另一套未公开初始化流程。严格恒边界工装可用于核对换热器局部方程，
但不能在没有输入时序证据时冒充图 5.19 的完整生成协议。

后续主线固定为：

1. **表 5.2 / 名义工作点：**用全系统检查 55090 rpm、压力/温度、功率、质量和能量
   闭合以及 14000 s 长期稳定性。
2. **图 5.18：**继续用单部件工装核对有储热状态的换热器方程、输入边界、初值和时间
   尺度，但明确这是对论文协议的一种可证伪实现，不自动等同于作者原始工装。
3. **图 5.19：**先识别四条功率曲线所需的驱动输入历史和初始状态。不得再用已稳态化
   全系统的平坦曲线，也不得使用本次已否定的跨谱系状态拼接；在协议未识别前不继续扫
   初值追图。
4. **§5.4：**只有前述名义端点、部件方程和功率驱动路径相容后，才把同一套正式部件
   接入全耦合动态模型；不从本次反向瞬态中调参追图。

❌ 作者完整部件初值、全部动态换热参数和部分冷却回路设计量仍未公开/未唯一识别。
因此当前只能继续做有来源的重构与可证伪实验，不能声称论文原始模型已恢复。

## 8. 图 5.19 功率形状所要求的上游温度历史

本节仍是不加载/不仿真 SLX 的条件反演。固定冻结模型的 `N=55090 rpm`、
`mdot=11.9820292904 kg/s`、压气机/透平查表和压力关系，只改变机器入口温度，逐点
求解固定 `paper_points.csv` 中的透平或压气机功率。固定量为：

```text
compressor PR  = 2.36202395391
compressor eta = 0.843706772475
turbine ER     = 2.28121785500
turbine eta    = 0.847643677116
```

❓ 这是“若其余机器输入和查表不变，需要什么入口温度才能产生该功率”的条件识别，
不是作者公开温度、不是闭环新工况，也不允许回写正式模型。尤其图 5.19 曲线平台与同页
正文/表 5.2 的透平功率存在可见差异，所以下列绝对温度不能用来替代论文表值；动态方向
和相对温度变化才是本检查的用途。

| 曲线 | 条件入口温度起点（10 s） | 极值 | 末点（495 s） | 所需形状 |
|---|---:|---:|---:|---|
| 透平功率 | 1437.513 K | 1492.596 K @ 150 s | 1492.596 K | 上升约 55.083 K |
| 压气机功率 | 408.324 K | 383.804 K @ 30 s | 399.545 K | 先降约 24.520 K，再回升 |

✅ 同时插值本次谱系组合候选的原始入口信号：

| 曲线 | 候选入口温度 @ 10 s | 候选入口温度 @ 495 s | 与论文功率所需方向比较 |
|---|---:|---:|---|
| 透平 | 1523.220 K | 1524.256 K | 基本平坦，缺少所需上升过程 |
| 压气机 | 545.220 K | 430.879 K | 先大幅升温后回落，方向相反 |

✅ 这与第 5 节直接功率分解一致：在当前固定转速/流量/查表下，图 5.19 的透平和
压气机动态形状主要要求不同的上游温度历史。对应当前拓扑，透平入口温度来自 IHX，
压气机入口温度来自预冷器；下一项应检查这两条上游热状态与初始化协议，不应先修改
TAC 功率公式、效率或查表来抵消错误温度历史。

❓ 图 5.18(c)所示冷却器热端出口总体为升温过程，而本条件反演的图 5.19 压气机入口
要求先降后升；因此也不能未经证据就把图 5.18 的独立部件输出直接当作图 5.19 的输入
回放。两图很可能使用不同的状态组合或耦合过程，但作者具体运行方式仍未公开。
