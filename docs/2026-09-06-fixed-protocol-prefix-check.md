# 固定更新协议后的共同时间窗验证

> 阶段记录：本文件的“当前”“本轮”“下一步”属于原写作时点；当前工作状态以 [STATUS.md](STATUS.md) 为准。原结论的证据等级及适用模型保持，不因本次目录整理升级。

**✅ 两次新的探索副本运行确认：固定MaxStep=10秒时，StopTime分别为500秒和1000秒，共同前500秒的80路导出日志时间网格一致，数值在CSV保存精度下逐点相同，无需插值。** 这验证了本次配置下的终止时间独立性，不是10秒步长充分性或论文验收。

## 执行边界

✅ 两组均复制当前31745b64…SLX，使用与既有基线相同的冻结运行依赖。只在探索副本内存中设置StopTime、MaxStep及日志记录；模型方程、查表、物性、初态和反馈均保持原状。编译后两条Unit Delay的周期均为[10,0]秒。

✅ 本轮实际执行500秒和1000秒两次整机探索运行；各自仿真调用耗时约2.842秒和1.363秒，不含MATLAB启动、编译和导出时间。无14000秒新运行。源模型与两个磁盘字节副本均未保存更改，12项受保护文件哈希一致，11项运行输入身份核对通过。

## 原始采样与比较结果

✅ 保存完整Simulink输出MAT，以及每路信号自身Time/Data的CSV。没有调用正式运行器的统一线性插值导出逻辑，没有以派生保持流量文件作为仿真输入。

| 检查 | 500秒运行 | 1000秒运行 |
|---|---|---|
| 编译压力/流量周期 | 10秒 | 10秒 |
| 延迟块原始日志网格 | 0:10:500，51点 | 0:10:1000，101点 |
| 截取共同前500秒的80路日志 | 时间网格及保存数值逐点相同 | 时间网格及保存数值逐点相同 |
| 为比较进行插值 | 无 | 无 |

✅ 这里比较的是导出CSV保存精度，不将“保存数值相同”扩大为原始浮点内存逐位相同。完整MAT已保留，可以用于后续其他核查。部分常量只有初始记录，比较器保留其实际记录数量，没有人为补造时间序列。

❓ 这一结果支持：在当前暖态初值和10秒最大步长配置下，延长终止时间不再改变共同前500秒的更新协议及已记录数值。它不证明全部工况均具有相同结果，也不代表已经做过步长减半收敛性验证。

## 热量失配仍然存在

❓ 使用本轮原始末点温度、堆功率及现有κ、γ、hA、固定cp重算：

| 项目 | 500秒末点 | 1000秒末点 |
|---|---:|---:|
| 堆功率 / kW | 2939.947512 | 2939.943034 |
| 反应堆出口温度 / K | 1587.217222 | 1587.217145 |
| 固定cp出口热量−燃料传热 / kW | **81.219446** | **81.219315** |
| 燃料储热 / W | −0.253714 | +0.002359 |

❓ 因此，更新协议问题与第一处81.219 kW内部热量差额确实是两件事。固定数值协议后，后者仍然存在，不能靠调采样周期消除，也不能将本轮共同时间窗一致称为能量修复。

## 可以沿用的工作条件

后续同类暖态诊断可明确沿用这一固定协议，并保存原始多速率日志，避免终止时间及线性插值同时混入对照。10秒仅作为已验证可比较的诊断设置；任何最终周期选择还须结合数值收敛与物理结构依据，不按曲线接近程度决定。

本轮完成有限范围的更新协议验证。正式模型保持只读；论文图5.18/5.19和第5.4节未通过新验收，整机复现未完成。

✅ [运行脚本](../tmp/fixed_protocol_prefix_20260906/run_pair.m)、[独立比较脚本](../tmp/fixed_protocol_prefix_20260906/verify_pair.py)、[80路检查结果及末点复算](../tmp/fixed_protocol_prefix_20260906/verification.json)。两组状态文件与原始数据分别在[500秒输出目录](../tmp/fixed_protocol_prefix_20260906/run_500)和[1000秒输出目录](../tmp/fixed_protocol_prefix_20260906/run_1000)。
