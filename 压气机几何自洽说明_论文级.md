# 压气机几何自洽说明（论文级）

> 目的：让 `hexe_compressor_lookup.mat` 中的压气机几何与论文《空间锂冷堆 He-Xe 布雷顿循环发电系统优化设计与运行特性分析》（徐驰，2022）的 **1 MWe 单轴 55090 rpm** 工况在物理上自洽，能经得起答辩质疑。
> 日期：2026-08-16

## 1. 之前发现的问题

| 项 | 原 `.mat` 值 | 问题 |
|---|---|---|
| `N_design` | 18732 rpm | 与论文单轴转速 **55090 rpm**、涡轮表、`InitialCondition=55090` 全部矛盾；且经拆 `final_steady_24a.slx` 确认 `N_design` 在模型里**零引用**（死元数据），模型实际用硬编码 `1/55090` 归一化 |
| `D2_m` | 0.435 m | 配 55090 rpm 叶尖速度 U₂ = π·0.435·55090/60 ≈ **1256 m/s**，远超离心压气机极限（≈350–550 m/s），物理上不可能 |

→ 已先修 `N_design` 18732.46 → **55090**（与模型归一化口径、单轴、涡轮、论文对齐）。

## 2. 论文 1 MWe 压气机设计工况（Table 2.1，已核对）

热力学节点温度/压力随功率等级近似不变（仅流量放大），论文 1 MWe 取值：

- 进口：**T₀₁ = 514 K**，**P₀₁ = 1.167 MPa**
- 出口：**T₀₂ = 695 K**，**P₀₂ = 2.241 MPa** → **PR = 1.92**
- 等熵效率：由 T₀₂ 反算 η_c = T₀₁(PR^((γ-1)/γ)−1)/(T₀₂−T₀₁) ≈ **0.846**（γ=1.667）
- 质量流量：**ṁ = 12.05 kg/s**（与 `turbine_table2` 的 `bp_mf=12.0494` 吻合，1 MWe）
- 单轴转速：**N = 55090 rpm**（§5.x 明确 1.000 MWe → 55090 转/分）

> 注意：`.mat` 原设计点（T_in=405 K、P_in=658 kPa、PR=2.357）**不是**论文给的，是外部 map 生成时填的参考机工况。

## 3. 几何反推（Euler 功 + 叶尖速度约束）

He-Xe 物性（来自 `.mat`）：R=207.86 J/(kg·K)，cp=519.65 J/(kg·K)，γ=1.667。

1. 实际比功  
   w = cp·T₀₁·(PR^((γ-1)/γ) − 1) / η_c  
   = 519.65 × 514 × (1.92^0.400 − 1) / 0.846 ≈ **9.42×10⁴ J/kg**

2. 取功系数 ψ ≈ 0.67，所需叶尖速度  
   U₂ = √(w/ψ) ≈ **376 m/s**（离心压气机合理范围）

3. 由转速反推叶轮直径  
   D₂ = 60·U₂ / (π·N) = 60×376 / (π×55090) ≈ **0.130 m**

4. 其余几何按相似律（线性）从原值缩放，缩放比 s = 0.130 / 0.43536 ≈ 0.299：
   - D1T = 0.268 × s ≈ **0.080 m**
   - D1H = 0.153 × s ≈ **0.046 m**
   - b3（扩压器出口宽）= 0.0235 × s ≈ **0.0070 m**
   - β3b=25°、Z=20、slip=0.644 为角度/叶数/系数，保持不变

## 4. 修正前后对比

| 量 | 原值 | 论文自洽值 | 说明 |
|---|---|---|---|
| N_design | 18732 rpm | **55090 rpm** | 与单轴/模型一致（已修） |
| D2_m | 0.435 m | **0.130 m** | U₂ 由 1256→**375 m/s** ✅ |
| D1T_m | 0.268 m | 0.080 m | 相似律缩放 |
| D1H_m | 0.153 m | 0.046 m | 相似律缩放 |
| b3_m | 0.0235 m | 0.0070 m | 相似律缩放 |
| β3b / Z / slip | 25° / 20 / 0.644 | 不变 | — |
| U₂ @ 55090 | 1256 m/s ❌ | 375 m/s ✅ | 物理合理 |

## 5. 已改动文件与备份

- 修改：`hexe_compressor_lookup.mat`（N_design、D2_m、D1T_m、D1H_m、b3_m）
- 备份：
  - `hexe_compressor_lookup.mat.bak` —— **最初原始**（含 N_design=18732）
  - `hexe_compressor_lookup.mat.bak2` —— 改几何前的当前态（N_design 已为 55090）

> 几何/设计标量为**死元数据**（在 `.m` 与 `.slx` 中零引用，不参与任何计算），故本次修改**不影响仿真结果**，仅为论文可追溯性。

## 6. ⚠️ 尚未解决的残留问题（需用户决策）

1. **Map 设计压比 ≠ 论文压比**  
   `PR_table` 在 speed_bp=1.0 / m_ratio=1.0 处 = **2.357**，对应 `PR_design=2.357`、`T_in_design=405`、`P_in_design=658 kPa`、`T_out_design=601.9`、`P_out_design=1.551 MPa`——均与论文 1 MWe 工况（PR=1.92、514 K、1.167 MPa、695 K、2.241 MPa）不符。  
   原因：该 map 是按另一台参考机（原 native N≈18732、D2=0.435）生成的，其"设计点"标签不是论文工况。  
   **影响**：`PR_table` 与 `ETAT_table` 是模型实际用于仿真的两张表。若要把**仿真结果**也对齐论文 PR=1.92，需把 `PR_table` 整体按 1.92/2.357≈0.814 缩放（保持 off-design 形状），这会**改变循环净功率/温度等结果**——属影响仿真的改动，需你确认后再做，不建议默默改。

2. **几何为推导值，非一手来源**  
   现 D2≈0.13 m 是按"论文工况 + 相似律"反推的初步设计值，与原始 0.435 机器形状相似。它解决了 U₂ 不可能的问题，但严格说仍无一手文献出处。若答辩要求几何可溯源，应查 **SP-100 的 BRU（布雷顿旋转机组）压气机初步设计**或已发表的 He-Xe/空间 Brayton 部件设计来坐实 D2、b2、叶型等。

## 7. 建议的下一步（按需要）

- **A（最小）**：保持现状——几何已物理自洽、仿真不变，仅在文档/答辩中说明 D2≈0.13 m 的由来（本文第 3 节）。
- **B（仿真对齐论文）**：在确认后把 `PR_table` 缩放至设计 PR=1.92，并同步更新 `PR_design` 等设计标量到论文值；重跑模型核对净功率。
- **C（彻底溯源）**：基于 NASA TN D-7487 用论文工况 + 本节几何**重新生成**压缩机 map（设计点=论文 1 MWe），做到 map 与设计点、几何三者完全一致。
