# AGENTS.md — 空间锂冷堆 He-Xe 布雷顿循环动态模型复现项目

本文件是本项目所有 Codex 线程与 Goal 的持久上下文。
Codex 开始处理本仓库中的任何任务前，必须先阅读本文件；本文件不是详细规范的替代品，详细规则以 `specs/` 下的正式文档为准。

---

## 1. 项目背景

- 论文：《空间锂冷堆He-Xe布雷顿循环发电系统优化设计与运行特性分析》（徐驰）
- 最终目标文件：`final_dynamic_24a.slx`
- 核心任务：复现论文第 5.4 节“动态运行特性分析”的仿真结果。
- 复现范围包括：5.4.1 启动瞬态，以及 5.4.2 中的变工况响应，包括 ±5% 转速扰动、TAC 负载扰动、反应性扰动。
- 数值和曲线的最终验收依据为 `验收标准_论文5.4.md`。

---

## 2. 必须遵守的正式规则文档

开始任何实质性修改、实验或最终验收前，必须阅读以下文档：

1. `specs/决策自律准则.md`
   - 任何结论、论文出处和证据判断必须使用其中的证据分级：✅ / ⚠️ / ❓ / ❌。

2. `specs/交付边界约束_v4.md`
   - 这是模型结构、黑名单、探索区/交付区、信号记录、反馈判断、出处要求和违规处理的正式约束。
   - **特别注意第 3.4 节：**转子动力学、反应堆温度负反馈等论文本身的物理耦合不是禁止的“控制反馈”；禁止的是为了拟合结果、稳定数值或缩小误差而额外增加的控制器、校正反馈或状态回注。

3. `specs/验收标准_论文5.4.md`
   - 这是数值和曲线验收阈值的正式依据。
   - 其中记录的论文内部矛盾、已核实数据和未核实数据必须按文档标记使用，不得擅自消解或修改验收标准。

如本文件与上述正式规则冲突，以更具体、更新的正式规则文档为准；不得自行创造新的交付规则。

---

## 3. 核心目标与优先级

本项目的目标不是“让曲线看起来像论文”，而是：

> **在不改变论文物理机制、不引入额外控制/校正/拟合机制的前提下，使最终交付模型通过第 5.4 节全部验收标准。**

执行时按以下优先级处理：

1. 物理模型与论文依据正确
2. 仿真结果能够通过验收
3. 修改具有可验证证据
4. 模型结构符合交付边界
5. 最终交付干净、可复现

数值结果更接近论文，**不能单独构成某项修改正确的证据**。

---

## 4. 已知事实与当前工作假设

### 4.1 已知项目结构

当前已知顶层部件（`system_root`）包括：

- `reactor`：含反应性输入 `rho_fw`
- `TAC`：含负荷输入 `Pload_fw`
- `recuperator`
- `IHX`
- `precooler`
- `radiator`（当前模型内实际拼写为 `rediator`）

这些部件是否最终保留，仍必须以 `交付边界约束_v4.md` 和实际模型结构核对为准。

### 4.2 当前重要工作假设

当前已有一个高可信的工作假设：

- 压气机表曾按 `PR=2.357`、`N_design=18732 rpm` 生成；论文实际参考点约为 `PR≈1.92`、`N_design=55090 rpm`，且当前转速断点未覆盖 `55090 rpm`。
- 该问题**很可能**是当前动态复现偏差的重要来源之一。

注意：以上属于**当前工作假设/已验证发现的候选根因**，不是不可推翻的最终结论。
Codex 必须允许后续证据推翻、修正或缩小该假设的适用范围，不得为了维持既有结论而选择性寻找证据。

任何 AGENTS.md 中的“已知事实”都不得替代直接检查；开始正式修改前，应确认相关事实仍然成立。

---

## 5. 核心执行原则

### 5.1 默认先观察，后修改

除非当前任务明确要求修改，否则默认：

- 不修改正式交付模型
- 不删除文件
- 不擅自重命名正式模型模块
- 不改变模型拓扑
- 不修改验收阈值
- 不增加控制器、校正反馈、拟合参数或曲线补丁

遇到问题时，优先建立事实基线、追踪信号和验证假设，而不是直接试错式修改正式模型。

### 5.2 假设—证据—实验—结论

对于涉及物理机制、模型结构或数值偏差的问题，优先使用以下闭环：

1. 明确问题现象
2. 提出可证伪的假设
3. 找到对应论文依据或模型证据
4. 在探索区进行最小化验证实验
5. 根据结果更新假设
6. 仅在证据充分时修改正式模型（具体执行协议见第7节）

不得把“能够运行”或“曲线更接近论文”直接等同于“物理正确”。
第7节不是另一套独立流程，是本节第6步“修改正式模型”这一步的具体展开——
走到第6步后直接接第7节执行，不要把两节当成先后要各走一遍的两套协议。

### 5.3 修改前说明

涉及正式模型的非平凡修改时，必须能够说明：

- 当前问题是什么
- 对应哪个物理部件、方程、图表或论文章节
- 修改的假设是什么
- 为什么这个修改是必要且尽量小的
- 修改后如何验证

出处要求不是“每一处改动都要能在论文里找到原文”这种绝对化标准，按
`交付边界约束_v4.md` 第5节的三分类执行：物理建模选择必须有论文出处，找不到先标记
为待确认；纯数值实现细节（求解器容差、插值方法、数值防护等）不强求论文出处，但
需声明“这是数值实现选择，不改变物理方程”；标定/拟合参数默认不允许，除非单独
说明并获得批准。不得把“论文没写实现细节”和“为了拟合结果而修改”混为一谈。

---

## 6. 探索区与交付区

### 6.1 探索区

`tests/`、`tmp/` 可以自由添加：

- 调试脚本
- 诊断输出
- 参数扫描
- 临时对照模型/子系统
- 临时绘图和数据分析
- 为定位问题而使用的辅助块

这些内容不受最终交付模型的第 3.3 / 3.5 节限制，但仍必须遵守基本安全原则，不得破坏原始工程。

### 6.2 交付区

最终交付模型必须是干净的正式模型：

- 不得引用 `tests/` 或 `tmp/`
- 不得包含仅用于调试、诊断、试探或拟合的模块
- 不得包含未经论文支持的新增物理机制
- 不得通过探索区脚本在运行时偷偷修改最终模型行为

探索区中的成功实验只能证明“某个假设值得进一步考虑”，不能自动成为交付模型的一部分。
只有能够对应论文物理模型并完成正式重构、验证和出处记录的内容，才允许进入交付区。

---

## 7. 修改—验证协议

任何影响正式模型行为的修改，都必须执行以下流程：

1. 记录修改前状态/基线
2. 明确修改内容
3. 说明修改原因和论文依据
4. 在可行情况下先于探索区验证
5. 应用最小修改
6. 重新运行相关场景
7. 对比修改前后结果
8. 对比论文验收指标
9. 检查是否产生新的副作用
10. 将结论记录到项目状态或实验日志中

禁止出现以下推理：

> “这次修改让曲线更像论文，因此修改就是正确的。”

除非有独立的物理/模型证据，否则只能判定为“结果改善”，不能判定为“模型正确”。

---

## 8. 原始基线与备份原则

在首次正式修改前：

- 先完成 Step 0 的带时间戳备份
- 先运行当前原始模型并记录其真实状态
- 不得因为“看起来应该这样”而跳过基线

基线的作用是区分：

- 模型原本就存在的问题
- 本次修改新引入的问题

具体备份命名、保存位置和交付排除规则以 `交付边界约束_v4.md` 为准。

---

## 9. Git 工作原则

Git 是本项目的版本控制和实验追踪手段，不只是备份盘。

建议将以下内容保持可追踪：

- 正式模型变化
- MATLAB/Python 脚本变化
- 关键参数变化
- 测试结果
- 重要决策
- 阶段性验收结果

在一个独立问题得到验证后，应尽量形成清晰、可回滚的提交。

不要把多个无关物理修改混成一个无法解释的大提交。

除非任务明确要求，不要为了“整理提交历史”而重写已有历史。

---

## 10. 不确定性与人工确认门

以下情况不能被 Codex 擅自决定：

- 是否加入一个新的物理机制
- 是否删除论文出处不明确的正式模型模块
- 是否加入可能属于控制器/校正反馈的机制
- 是否改变论文建模假设
- 是否使用标定/拟合参数直接修正结果
- 是否修改正式验收标准

遇到这些情况，必须：

1. 停止该方向的正式修改
2. 保留当前证据和实验结果
3. 明确说明争议点
4. 给出支持与反对证据
5. 请求人工判断

对于普通的代码错误、路径错误、语法错误、明显的端口错误、测试脚本错误和临时调试问题，不应因为上述人工确认机制而无谓停滞；在不改变物理假设的情况下可以自主修复。

---

## 11. 推荐的项目状态文件

如果仓库中尚不存在，建议维护：

- `docs/MODEL_MAP.md` —— 当前模型结构、信号流和模块对应关系
- `docs/STATUS.md` —— 当前进度、已通过/未通过项目和当前阻塞点
- `docs/PLAN.md` —— 阶段性执行计划
- `docs/DECISIONS.md` —— 需要长期保留的重要判断和人工决策
- `docs/EXPERIMENT_LOG.md` —— 实验、假设、修改和结果记录

这些文件是项目状态记录，不是新的物理规范；若与正式规范冲突，以 `specs/` 文档为准。

**范围限定**：范围明确、已有高可信根因假设的窄任务（例如当前压气机表修复），
不强制先建立这一整套文件才能开始动手；用简短的进度说明即可。展开到5.4.2多个
工况场景、需要跨多轮会话追踪时，再按需建立，避免把维护文档本身变成任务负担。

---

## 12. 完成定义（Done Criteria）

除非以下条件全部满足，不得宣称任务完成：

1. 论文第 5.4.1 节启动瞬态通过 `验收标准_论文5.4.md`
2. ±5% 转速扰动通过验收
3. TAC 负荷扰动通过验收
4. 反应性扰动通过验收
5. 所有要求的数值/曲线误差满足正式验收标准
6. `final_dynamic_24a.slx` 符合 `交付边界约束_v4.md`
7. 最终模型没有额外控制器、校正反馈、拟合补丁或调试模块
8. 最终模型不依赖 `tests/`、`tmp/` 或其他探索区残留
9. 重要物理建模选择具有论文出处和证据等级
10. 输出最终模型清单、依赖检查和验收结果
11. Step 0 备份仍然存在且未被覆盖/删除

某项未通过时，应明确报告“未完成”以及阻塞原因，不得为了达到 Done 状态而修改验收标准、隐藏失败结果或绕过物理约束。

---

## 13. 启动任何新任务前的最小检查

开始新的 Codex 线程或 Goal 时，先确认：

1. 已阅读本 `AGENTS.md`
2. 已阅读本任务涉及的 `specs/` 正式规则
3. 已确认当前 Git 工作树状态
4. 已确认当前任务是在探索区还是交付区
5. 已确认当前基线是否存在
6. 已确认本任务的“完成条件”是什么

如果任务信息不足，优先检查仓库现有事实和文档；不要凭空补全关键物理假设。
